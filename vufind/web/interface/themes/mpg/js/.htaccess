<IfModule mod_rewrite.c>
  RewriteEngine Off
</IfModule>

RemoveType .js
AddType text/javascript .js 

RemoveType .css
AddType text/css .css

# Dynamic JS (allow execution of PHP code in JS files / zim)
AddType application/x-httpd-php .js